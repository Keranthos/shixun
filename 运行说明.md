# 软工资源平台 · 运行说明

本文档说明如何在**本地**启动本项目（团队日常开发方式）。仓库根目录为 `shixun`。

> `docker-compose.yml` 仅作课程演示参考，**本地开发无需 Docker**。

## 项目结构

| 目录 | 说明 |
|------|------|
| `softeng-platform/softeng-platform/` | Go 后端 API（默认 `:8080`） |
| `softeng-platform-frontend/` | Vue 3 前端（开发默认 `:3000`） |
| `agent/` | LangChain RAG Agent（默认 `:8766`，可选） |
| `run-dev.ps1` / `run-dev.bat` | 本地一键启动脚本 |

---

## 环境要求

| 组件 | 版本建议 | 用途 |
|------|----------|------|
| MySQL | 8.0 | 业务数据库 `softeng` |
| Go | 1.23+ | 编译与运行后端 |
| Node.js | 16+ | 前端开发与构建 |
| npm | 随 Node | 安装前端依赖 |
| Air | 可选（脚本可自动安装） | 后端热重载 |
| Python 3.10+ | 可选 | RAG Agent |
| conda | 可选 | `run_dev.ps1` 优先使用；无 conda 时可用系统 Python |

---

## 1. 初始化数据库（首次或空库）

确保 MySQL 已启动，在客户端中依次执行以下 SQL（路径：`softeng-platform/softeng-platform/database/`）：

1. **`bootstrap_user.sql`** — 创建应用账号 `softeng_app`（推荐，与后端默认配置一致）
2. **`schema.sql`** — 建库建表
3. **`seed_demo.sql`** — 导入演示数据（工具 / 课程 / 项目 / 演示账号）
4. **`seed_course_content_all_19.sql`** — 为当前库全部课程（按 `course_id`）写入「内容摘要」「学习路径」（需先执行第 5 步加字段）
5. **`patch_project_content_insight.sql`** + **`seed_project_content_all_8.sql`** — 为全部已审核项目写入「内容摘要」（8 条演示项目）

若已有旧库需对齐演示数据，可额外执行 **`patch_existing_to_demo.sql`**。  
若库中尚无 `content_insight` / `learning_path` 字段，先执行 **`patch_course_content_insight.sql`**，再执行 **`seed_course_content_all_19.sql`**（覆盖 19 门课，含高等数学/C 语言等通识与专业课）。  
项目摘要：先 **`patch_project_content_insight.sql`**，再 **`seed_project_content_all_8.sql`**（CMD UTF-8 导入，勿用 `Get-Content | mysql`）。

**Windows 注意**：不要用 `Get-Content | mysql` 导入含中文的 SQL，易变成 `????`。请用仓库根目录脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\reimport_course_content.ps1
```

或 CMD：`chcp 65001` 后 `mysql ... --default-character-set=utf8mb4 softeng < database\seed_course_content_all_19.sql`。

也可使用 root 账号：在后端 `.env` 中把 `DB_USER` / `DB_PASSWORD` 改为 root 对应值即可。

检查表是否就绪（可选）：

```powershell
cd softeng-platform\softeng-platform
go run .\cmd\dbcheck\main.go
```

---

## 2. 配置后端

```powershell
cd softeng-platform\softeng-platform
copy .env.example .env
# 编辑 .env，至少核对 DB_* 与 JWT_SECRET
```

关键项见 **`.env.example`**（推荐 `DB_USER=softeng_app`，密码 `123456`，需先执行 `bootstrap_user.sql`）。

---

## 3. 配置 RAG Agent（可选）

完整向量检索问答需启动 Python Agent：

```powershell
cd agent
copy .env.example .env
# 编辑 .env，填入 GOOGLE_API_KEY
# Gemini 默认自动读取 D:\blog\agent\.env（与 blog 相同，一般无需再抄一份）
# blog 不在 D盘时：在 shixun/agent/.env 写 BLOG_AGENT_ENV=D:\你的路径\blog\agent\.env
pip install -r requirements.txt
```

`agent/.env` 关键项：

```env
AGENT_PORT=8766
GOOGLE_API_KEY=你的Gemini密钥
CHROMA_PERSIST_DIR=./data/chroma
```

在后端 `.env` 中同时设置 `AGENT_BASE_URL=http://127.0.0.1:8766`。

---

## 4. 一键启动（推荐）

### 日常开发（更快）

若每次启动要 **5～10 分钟**，优先用快速脚本（已跳过 dbcheck、重复 pip、长等待 Agent）：

```powershell
powershell -ExecutionPolicy Bypass -File .\run-dev-quick.ps1
```

或：

```powershell
.\run-dev.ps1 -SkipDbCheck -SkipAgentPip -AgentWaitSec 12
```

后端 `.env` 建议 **`AGENT_AUTO_REINDEX=false`**（避免起后端就在后台全量建索引拖慢机器）。

### 完整启动

在仓库根目录 `shixun` 执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\run-dev.ps1
```

或双击 **`run-dev.bat`**。

脚本会：

1. （可选）运行 `dbcheck` 检查数据库表（每次会 `go run`，约 10～30 秒）  
2. 在新窗口启动 **后端 Air**（热重载）  
3. 若存在 `agent/.env`，启动 **Agent（8766）**；依赖已装过则 **跳过 pip**（见 `agent/.pip_ok`）  
4. 等待 Agent `/health`（默认最多 **45 秒**，不再阻塞 2 分钟）  
5. 在新窗口启动 **前端** `npm run serve`（**仅首次**无 `node_modules` 时才 `npm install`）

**会开 3 个服务窗口**（后端 / Agent / 前端）。Agent 窗口出现 `Uvicorn running on http://127.0.0.1:8766` 即可用学习助手。

### 启动慢常见原因

| 环节 | 耗时 | 处理 |
|------|------|------|
| 首次 `npm install` | 数分钟 | 只需一次；之后用 quick 脚本 |
| 每次 Agent `pip install` | 1～3 分钟 | 已支持跳过；`AGENT_FORCE_PIP=1` 可强制重装 |
| `dbcheck` 的 `go run` | 10～30 秒 | `-SkipDbCheck` 或 quick 脚本 |
| 等待 Agent 120 秒 | 最多 2 分钟 | 已改为 45 秒；quick 为 12 秒 |
| 前端 Webpack 冷启动 | 1～3 分钟 | 窗口先出来，编译完再访问 3000 |
| `AGENT_AUTO_REINDEX=true` | 后台很久 | 改为 `false`，手动重建索引 |
| Cursor 打开整个仓库 | 视机器而定 | 设置里排除 `node_modules`、`agent/data` 索引 |

启动参数：

```powershell
.\run-dev.ps1 -SkipDbCheck           # 跳过数据库表检查
.\run-dev.ps1 -SkipAgent             # 不启动 LangChain Agent
.\run-dev.ps1 -SkipFrontend          # 仅启动后端
.\run-dev.ps1 -SkipAgentPip          # Agent 不重复 pip
.\run-dev.ps1 -AgentWaitSec 0        # 不等待 Agent，立刻继续
```

---

## 5. 访问地址

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:3000 |
| 后端 API | http://localhost:8080 |
| 健康检查 | http://localhost:8080/health |
| RAG Agent | http://127.0.0.1:8766 |

### 演示账号

密码均为 **`123456`**（来自 `seed_demo.sql`）：

| 用户名 | 角色 | 说明 |
|--------|------|------|
| `admin` | 管理员 | 审核中心、洞察页、索引重建等 |
| `student_zhang` | 学生 | 普通浏览、收藏、RAG 问答等 |
| `teacher_zhang` | 教师 | 普通用户能力 |

### 常用页面

- 首页：`/home`（侧边栏：常用 / 工具 / 课程 / 项目；**登录后**可见 RAG 助手）
- 工具 / 课程 / 项目：`/tools`、`/course`、`/projects`
- 学习助手（RAG）：`/assistant/rag`（登录即可；管理员可在页内重建索引）
- 旧链接 `/insights/rag-agent` 会自动跳转到 `/assistant/rag`
- 管理员「管理工具」折叠区（需 `admin` 登录）：
  - 审核中心：`/check/audit`
  - 运营大屏：`/insights/dashboard`
  - 接口监控：`/insights/telemetry`
  - 资源目录：`/insights/long-list`
  - 关联图谱：`/insights/knowledge-graph`
- 个人行为与动态（登录用户）：`/insights/session-persona`、`/insights/event-timeline`

---

## 6. 手动分步启动（排查问题用）

**终端 1 — 后端**

```powershell
cd softeng-platform\softeng-platform
air
# 若无 air：go run .\cmd\server\main.go
```

**终端 2 — 前端**

```powershell
cd softeng-platform-frontend
npm install    # 首次
npm run serve
```

**终端 3 — Agent（可选）**

```powershell
cd agent
.\run_dev.ps1
# 无 conda 时：python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8766
```

---

## 常见问题

### 数据库连接失败

- 确认 MySQL 服务已启动  
- 核对 `.env` 中 `DB_*` 与本地账号密码（默认 `softeng_app` / `123456` 需先跑 `bootstrap_user.sql`）  
- 确认已执行 `schema.sql`，且库名 `softeng` 存在  

### 前端能开但列表为空

- 确认后端 `:8080` 已启动：`curl http://localhost:8080/health`  
- 确认已导入 `seed_demo.sql`  

### RAG 超时 / 一直「思考中」

**常见根因**：Agent 与 Go 回退都会调用 **Google Gemini**（`generativelanguage.googleapis.com`）。国内网络直连该域名常会 **连接超时**，表现为左下角助手长时间无响应。

**排查**（PowerShell）：

```powershell
# 后端与 Agent 进程是否在跑
curl http://localhost:8080/health
curl http://127.0.0.1:8766/health
```

**处理顺序**：

1. 确认 **Agent 8766 窗口未关闭**；向量索引为 0 时，管理员在学习助手内 **重建索引**（或 `POST /api/agent/reindex`）。
2. 若仍超时：在 `agent/.env` 与后端 `.env` 中配置 **Gemini 中转**（与 blog/agent 相同方式）：
   - `GEMINI_API_BASE=https://你的反代域名`（不要用 Google 官方域名）
   - `GEMINI_PROXY_TOKEN=你的反代口令`
3. 或使用 **VPN / 全局代理** 后再访问 Gemini。
4. 无 Key、无代理时，Go 会回退 **MySQL 关键词 + demo 模板**，界面显示「简化检索模式」，回答质量有限但不应无限卡住。

**说明**：索引为空时 Agent 已优化为跳过 embedding；有索引后 embedding/生成仍依赖 Gemini 可达。

**「开了梯子仍报 SSL EOF」**：浏览器走代理，但 **Agent 是独立 Python 进程**；**blog 的 `agent/.env` 不会自动给 shixun 用**。任选其一：

1. **与 blog 相同（推荐）**：保持 `D:\blog\agent\.env` 里已有的反代配置；shixun Agent **启动时自动继承**（勿在 shixun `.env` 里写死官方 `GEMINI_API_BASE` 覆盖掉）  
2. **本地 Clash 直连 Google**：仅在 shixun `.env` 设 `GEMINI_HTTPS_PROXY=http://127.0.0.1:7890`，且 `GEMINI_API_BASE` 为官方域名

保存后 **重启 Agent 窗口**；`curl http://127.0.0.1:8766/health` 里 `gemini_proxy` 非 null，或日志有 `[gemini-http] 使用代理` / 反代 base 已配置。

### RAG 显示「已降级」

- Agent 未启动或未配置 `AGENT_BASE_URL` 时，Go 会回退到 LIKE + LLM/demo，属正常  
- 启动 `agent/` 并在后端 `.env` 设置 `AGENT_BASE_URL=http://127.0.0.1:8766`  
- 索引为空时管理员可在 `/assistant/rag` 页内重建，或调用 `POST /api/agent/reindex`，或设置 `AGENT_AUTO_REINDEX=true`  
- **额度**：默认重建**不会清空**已有向量，相同 `chunk_id` 且正文未变会**跳过 embedding**；仅换模型或索引损坏时用 `POST /api/agent/reindex?clear=true`  
- 重建前看 `GET /api/agent/status` 的 `chunk_count`；Agent upsert 响应 `meta.embedded_new` 为本次实际调用 embedding 的条数  

### 找不到 `air`

```powershell
go install github.com/air-verse/air@latest
```

将 `%USERPROFILE%\go\bin` 加入系统 PATH 后重试 `run-dev.ps1`。

### 端口被占用

- 前端默认 `3000`，后端 `8080`，Agent `8766`  
- 修改后端：`PORT` 环境变量；修改 Agent：`agent/.env` 中 `AGENT_PORT`  

---

## 生产构建（参考）

```powershell
# 后端
cd softeng-platform\softeng-platform
go build -o server.exe .\cmd\server\main.go

# 前端
cd softeng-platform-frontend
npm run build
# 产物在 dist/，可由 nginx 等静态托管
```

---

## 相关文档

- `README.md` — 后端架构与技术说明  
- `agent/README.md` — RAG Agent API 与配置  
- `高级功能与报告亮点清单.md` — 功能清单与答辩要点  

---

*最后更新：2026-06-01*
