# 软工资源共享平台 · 答辩讲稿

## 第 1 页

评委好，我是万伟杰；今天由我来介绍我们小组的实训项目：软工资源共享平台。

## 第 2 页

接下来，我将从项目背景、代码说明、功能介绍、技术亮点等方面介绍我们的实训项目，中间穿插演示项目的效果。

## 第 3 页

首先说明一下我们为何要做这个项目。因为我们在日常的学习生活中经常遇到这些问题：有时我们急需完成任务，但是找不到好用、免费的相关工具；或者在学期中完成作业时，时常苦于课程文档散落在各个课程群中，期末时还可能会出现课件过期的情况；时常有人做出来一个好的项目，想要一些 star，却不知道如何推广……以及诸如此类的问题。因此我们想构建一个专注于信息收集、保存与分享的网站，服务学院的老师同学们。

## 第 4 页

当然我们已经有了软工集市这个很好的项目，不过它依然存在一些限制：以论坛的模式来进行资源的分享，势必会出现时效性的问题，也就是老帖子会被新帖子淹没；而对于有一些资源来说比如课程的相关资料与课件，大家觉得并没有必要发在论坛中……所以我们认为集市更像是一个交流的平台，而我们同样需要一个展示的平台。

## 第 5 页

因此我们希望完成一个网站，聚焦于分享与记录同学们日常学习生活中发现的好用工具，自己的项目，以及各种各样课程的资料与评价等等应该被沉淀下来的信息，用以服务学院的所有师生。

## 第 6 页

首先是团队的分工。我们小组共有六人，大概是分为了前端三人与后端三人。由我来完成相关前后端对接、智能体与工作流工具开发。

## 第 7 页

接下来大致讲解一下项目的代码。后端使用的是 Go 语言，Gin 框架进行 Web 开发，原生 SQL 加 Repository 数据访问层则处理与数据库的连接。

大致可以划分为以下这五层：首先最靠近数据库的是 Model 层，负责建立对应的数据结构与实例，存储从数据库中提取的数据；然后是 Repository 层，用于封装所有的数据库操作与数据库连接查询；接下来是 Service 层，负责实现业务核心逻辑，比如用户认证、热榜、搜索等等功能；然后是中间件层，负责全局的通用逻辑，比如跨域与 JWT 认证；最顶端是 Routes 层，负责将前端的 HTTP 请求绑定到不同的处理函数中。当然除此之外还有 MySQL 数据库、文件系统等等组件，这里不做赘述。

## 第 8 页

前端使用的是 Vue 3 框架，同样也可以分为五层。工具层提供通用的工具函数，比如待会会提到的图片 URL 处理与默认头像生成；Store 层负责存储全局的状态管理；API 则封装 HTTP 请求，管理 API 接口；视图与组件则主要负责前端界面的展示，比如工具列表页、详情页、提交页等等；最后也是 Routes 层，负责配置前端的路由。整体按工具、课程、项目、个人中心、管理端等模块组织页面。

## 第 9 页

下面进入功能部分，我直接来进行演示吧。

首先是工具资源模块，用户可以按分类、标签浏览工具列表，也可以按最新、浏览量、收藏量等方式排序；

支持关键词搜索，点进详情页能看到完整的使用说明，包括简介、收藏数、浏览量、相关工具以及评论等等，同时也支持用户的这些功能；

登录后可以提交新工具，填写名称、链接、描述、分类和标签

## 第 10 页

第二个是课程路线模块，此处是按照培养计划与年级来划分。

选课前后，同学往往想知道一门课评价如何、历年资料在哪里。

课程列表支持按学期、分类筛选，也有多种排序方式；

详情页展示教师信息和资源列表，课程的内容摘要、学习路径等等，同时也支持大家上传网盘链接或文件资料；

此处也可以看到、分享评论

## 第 11 页

第三个是项目分享模块。

同学做完实训或竞赛项目，往往希望有人能看到、能 star。

列表支持分类和技术栈筛选，也支持收藏和评论。

详情页中可以展示和提交软件项目，包括 GitHub 链接、技术栈、项目简述、demo图片等等；

## 第 12 页

第四个是个人中心模块。

用户在这里编辑昵称、头像和个人描述；

在「我的提交」里查看自己提交的工具、课程、项目，以及待审核、已通过、已驳回等状态（因为这个是管理员，所以很多数据都是通过该账号输入数据库）；

在「我的收藏」里统一管理收藏内容；

也可以修改邮箱和密码保证账户安全。

## 第 13 页

第五个是管理审核模块，这个是独属于管理员账号的。

管理员在审核中心查看所有待审的工具、课程和项目，可以逐条通过或驳回，驳回时需要填写原因；也支持批量审核。页面上还有审核流程的状态机示意，方便理解从用户提交、进入待审队列，到管理员裁定通过或驳回的完整链路。

## 第 14 页

接下来是数据中控台，在首页侧栏「管理工具」里可以进入数据概览；

数据概览页用 KPI 卡片展示资源总量和待审队列，下面用图表看类型占比和工具分类排行，方便判断平台哪类内容偏多、审核是否积压

同一模块里还有资源关联图谱：根据用户提交时填写的标签，计算资源之间共享了多少标签，连线越粗表示关联越强，点击节点可以直接进详情，帮助发现「这门课常和哪些工具、项目一起被使用」



追问补充：

数据概览页使用 Apache ECharts 做饼图和柱状图，指标数据通过前端并行请求工具、课程、项目列表及待审核接口，在浏览器侧做计数与分类聚合后渲染。我们没有单独建数仓，而是直接基于业务库已有数据做轻量运营统计，优点是实现成本低、与线上一致；目前课程项目量级完全够用。

平台里浏览量、收藏数等采用 SQL 子查询实时统计，而不是单独维护易失效的缓存字段，保证详情页、列表页与运营视图看到的数据一致。



在接下来是RAG问答智能体：

在全站左下角可以打开学习助手，也可以进入专门的学习助手页面，用自然语言提问。助手

基于我们平台里已有的工具、课程、项目、评论和课程资料做检索后再生成回答；

比如：

回答里会带引用编号，点进去能跳到平台内对应条目，避免「看起来对但找不到出处」的问题；

管理员还可以在索引页看 Agent 是否就绪、向量条数，并在内容大批量更新后重建索引。

## 第 15 页

接下来讲一下是工程化实践。资源共享平台有很多需要上传图片的图片，而且支持填写图片 URL 而非只上传文件，因此带来了问题：外部图片服务可能会关闭或删除图片，图片 URL 可能会变更或者过期，攻击者也可能通过该链接进行注入攻击等等。

我们的做法是外链图片本地化：自动识别并处理图片传输格式；智能下载外部图片并保存到本地文件系统，按年月组织目录结构；Base64 图片自动解码并转换为文件存储；前端有统一的 URL 处理层，解决跨域和路径问题；文件命名采用时间戳加 MD5 哈希，避免重名冲突。

另外，对于用户可能不上传图标的情况，如果所有占位符都统一用外部默认图片，同样可能面临失效、加载慢等问题。我们基于昵称或项目名称首字符，用颜色分配算法自动生成 SVG 格式图标，几乎零存储成本，和用户自己上传的图标可以无缝切换。

## 第 16 页

在功能迭代中我们发现，工具、课程、项目等界面的展示、评论、提交的接口与逻辑存在大量重合。因此为了加速开发，我们自研了代码生成工具链：从 database/schema.sql 自动解析数据库表结构，自动生成 Go 语言的 Repository 接口和实现代码，包含基础 CRUD 操作；同时集成 Swagger，从代码注释生成可视化 API 文档。这减少了重复劳动，也保证了接口契约一致，显著提升了迭代效率。









## 第 17 页

RAG 是我们本期最重要的增量能力。登录用户在全站左下角可以打开学习助手，也可以进入专门的学习助手页面，用自然语言提问。助手不是泛泛聊天，而是基于我们平台里已有的工具、课程、项目、评论和课程资料做检索后再生成回答；Go 后端做鉴权和转发，Python Agent 服务负责向量检索和大模型生成，仓库里 agent 目录和 run-dev 脚本可以一起拉起。回答里会带引用编号，点进去能跳到平台内对应条目，避免「看起来对但找不到出处」的问题；管理员还可以在索引页看 Agent 是否就绪、向量条数，并在内容大批量更新后重建索引。

## 第 18 页

技术上，RAG 的核心是「先检索、再生成」：用户问题不直接丢给大模型，而是先从平台语料库中检索相关片段，再把片段作为上下文交给 LLM 组织回答。相比纯聊天，RAG 答案更贴近站内真实内容，幻觉更少，也便于用引用编号溯源。我们的语料包括工具说明、课程与项目描述、评论、课程资料等，索引由 Go 侧触发全量或增量重建，Agent 侧用 Chroma 存向量、SQLite 做倒排，形成混合检索。

纯向量检索擅长语义相近，比如「容器部署」和「Docker」；纯关键词检索擅长精确匹配，比如课程号、工具名。混合检索综合两者排序，提高召回与精度。对教学平台来说，用户提问方式多样，混合检索比单一方案更稳；因此我们把 Python Agent 单独拆出来，Go 负责 JWT 鉴权、限流、与现有业务同端口暴露，Agent 专注 LangChain 编排、embedding 和流式输出，主业务不受 Python 依赖影响，智能能力可插拔。

问答采用 SSE 流式返回，用户能看到回答逐字出现，而不是长时间白屏等待；Go 转发 Agent 的 stream，前端按 chunk 渲染 Markdown，答辩演示时体验也更流畅。

---

