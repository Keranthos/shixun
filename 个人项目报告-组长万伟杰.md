# 软件工程专业综合实训 · 个人项目报告

| 项目 | 内容 |
|------|------|
| **姓名** | 万伟杰 |
| **学号** | （请填写） |
| **班级** | （请填写） |
| **项目名称** | 软工资源平台（SoftEng Platform） |
| **项目成员** | 万伟杰、蔡沛良、杨凯宇、龚虹宇、高颖轩、唐云鹏 |
| **代码仓库** | GitHub: Keranthos/shixun |

---

## 摘要

软工资源平台是面向学院师生的教学资源聚合 Web 系统，整合工具、课程、项目三类资源的浏览、提交、审核与互动，并扩展运营数据大屏、资源关联图谱与 RAG 学习助手。系统采用前后端分离架构，日常业务由 Go 与 MySQL 承担，RAG 智能体以独立 Python 服务运行。主要工作包括：审核通过后自动更新问答索引、向量与关键词混合检索、标签关联图谱，以及图片本地化、一键启动等工程化支持。

**关键词**：软工资源平台；RAG 智能体；混合检索；LangChain；数据可视化

---

## 一、项目总述

### 1.1 项目背景

在软件工程专业教学实践中，工具链接、课程资料与优秀项目案例往往分散于即时通讯群组、网盘及个人收藏之中，存在检索效率低、链接易失效、难以形成长期沉淀等问题。现有论坛类平台侧重即时交流，对需持续维护的工具说明、课程课件及项目展示类内容支撑不足。

### 1.2 建设目标与主要功能

本项目旨在构建面向学院师生的软工资源共享平台，实现工具、课程与项目三类信息的统一收集、展示、提交与审核发布。主要功能包括：资源浏览与关键词搜索、收藏评论与提交审核、管理员运营数据大屏、资源关联图谱，以及基于平台已有内容的 RAG 智能问答。

### 1.3 系统架构

系统由浏览器、Go 后端、Python 智能体服务及多套数据库组成。用户的一般请求先由 Go 后端处理鉴权和业务逻辑；使用学习助手提问时，Go 后端将请求转发给 Python 智能体，智能体从知识库检索相关内容后交给大模型组织回答，再经 Go 后端流式返回浏览器。日常业务数据存在 MySQL；RAG 智能体另建向量库和关键词索引，分别使用 Chroma 与 SQLite。

系统按职责分为四层，如表 1 所示。

**表 1 系统分层架构**

| 层次 | 做什么 | 主要技术 |
|------|--------|----------|
| 前端层 | 页面展示、图表、用户操作 | Vue 3、Element Plus、ECharts |
| 后端层 | 资源管理、审核、登录鉴权、转发智能体请求 | Go、Gin、JWT |
| 智能体层 | 建知识库、检索、调用大模型生成回答 | Python、FastAPI、LangChain |
| 数据层 | 存业务数据和检索索引 | MySQL、Chroma、SQLite |

RAG 智能体内部各环节所用技术如表 2 所示（与表 1 的分层不是同一维度，而是智能体内部的实现拆分）。

**表 2 RAG 智能体技术构成**

| 环节 | 采用方案 | 说明 |
|------|----------|------|
| 内容切块与同步 | 后端自主实现 | 从 MySQL 读取已审核内容，按固定长度切块，审核通过后自动更新 |
| 文本向量化 | Gemini 嵌入接口 | 把文字变成向量，供语义搜索使用 |
| 语义搜索 | Chroma 向量库 | 按意思相近找相关内容 |
| 关键词搜索 | SQLite 及自研模块 | 按工具名、课程名等精确匹配，采用 BM25 类打分 |
| 两路结果合并 | 自主实现 | 把语义搜索和关键词搜索的结果加权合并 |
| 对话流程 | LangChain | 组织「先检索、再拼上下文、再调模型」的流程 |
| 生成回答 | Gemini 生成接口 | 基于检索到的内容生成回答，并标注引用编号 |
| 流式输出 | SSE 经 Go 后端转发 | 回答逐字返回，不用等全部生成完 |

工程部署采用 Docker Compose 编排 MySQL、API 与前端静态服务，并通过 Monorepo 与一键启动脚本方便本地运行。

### 1.4 搜索相关功能的三块划分

平台里和「找内容」有关的功能分三块，互不替代：

- **首页与搜索页的关键词搜索**：靠同义词规则和 MySQL 数据库查询，面向「已知关键词、快速筛列表」；
- **详情页的相关推荐与重复投稿提示**：靠标签相似度等轻量算法，在用户浏览或提交时给出关联与去重提示；
- **RAG 学习助手**：靠向量搜索与关键词搜索混合检索后再让大模型回答，面向自然语言提问。

---

## 二、个人工作说明

### 2.1 工作概况

本人担任项目组长，工作贯穿**中期前基础集成、中期后智能化拓展、期末验收工程化**三阶段，与组内同学共同完成需求分析、数据库设计、前后端开发、联调测试与文档整理。个人投入最集中的方向包括：

- 前后端集成与接口规范；
- 工程化工具链；
- RAG 学习助手（索引、检索、前端问答）；
- 运营可视化（Insights 管理端）；
- 智能检索与内容安全；
- Monorepo 与 Docker 交付。

按时间线概括如下：

- **中期前**：统一 OpenAPI 契约与 Swagger 文档，封装前端 HTTP 请求层，编写 Repository 脚手架与图片本地化逻辑，跑通浏览、收藏、评论、提交、审核等主流程。
- **中期后**：设计并实现 RAG 学习助手（Go 切块与网关、Python 智能体、混合检索、SSE 流式问答），建设 Insights 运营大屏与关联图谱，补充智能搜索意图识别、内容安全双端校验、run-dev 一键启动与 Air 热重载。
- **验收期**：git subtree 合并 Monorepo，Docker Compose 三服务编排，dbcheck 自检与《运行说明.md》，去除 Demo 化占位数据，保证答辩时可稳定演示。

### 2.2 问题、创新与技术方案

#### （1）平台内容多，但无法用自然语言可靠问答

**面临的问题。** 工具、课程、项目沉淀在列表和详情页里，同学往往要逐页翻找；若直接调用大模型，又容易脱离站内真实内容「瞎编」，且审核新上架的资源无法立刻被问答到。此外，主站业务与智能问答若耦在同一进程，一旦嵌入模型或向量库异常，可能影响整体 API 可用性。

**创新思路.**

- 学习助手只根据本站已审核内容回答，并标注引用来源；
- 资源审核通过或被修改后，同步更新问答用的索引；
- Go 负责业务和请求转发，Python 负责检索和生成，两个服务分开部署，互不影响。

**技术方案.**

- Go 从 MySQL 读取已审核文本（含评论与课程资料）并切块，内容变更时更新索引，管理员也可手动重建；
- Python 侧用 Chroma 存向量、SQLite 做关键词索引；
- 用 LangChain 组织检索与生成流程，Gemini 负责嵌入和回答；
- Go 后端做登录校验、限流，用 SSE 把回答流式返回前端；
- 若智能体或 API 不可用，依次改为只返回检索结果、再用 SQL 关键词搜索；
- 前端用悬浮面板做多轮对话，引用编号可点开详情。

#### （2）用户提问方式多样，单一搜索路径召回不稳

**面临的问题.** 只走向量搜索，搜工具名、课程号等短词容易不准；只做数据库模糊匹配，又听不懂「换一种说法」的提问。首页列表搜索与学习助手面向的场景不同，若混为一谈，既浪费算力，又难以向用户解释「为什么两种入口结果不一样」。

**创新思路.** RAG 问答同时做向量检索和关键词检索，自行合并结果，并限制同一资源出现的次数；首页搜索继续用同义词和意图判断，与学习助手分开做，各管各的场景。

**技术方案.**

学习助手收到提问后，智能体同时查询 Chroma 向量库与 SQLite 关键词索引：向量路负责理解语义相近的表述，关键词路负责精确匹配工具名、课程名等短词，两路结果按相似度加权合并，并限制同一资源最多引用几段。

首页搜索则走另一套路径，主要包括：

- **同义词扩展**：维护同义词对照表，用户输入「IDE」时自动扩展为「开发环境」「编辑器」等相关词；
- **检索意图识别**：根据查询内容判断用户更可能想找工具、课程还是项目，自动切换到对应标签页；
- **搜索状态与 URL 同步**：搜索词写入浏览器地址栏，刷新或分享链接后仍能保持同一搜索状态。

底层调用 Go 后端的 MySQL 列表接口做模糊匹配。详情页按标签相似度推荐同类资源；首页搜列表、详情页做推荐、学习助手做问答，三者用途不同。

#### （3）协作开发与资源展示存在多处工程痛点

**面临的问题.** 用户填写的外链图片易失效且存在跨域风险；工具、课程、项目三套模块 CRUD 高度相似，手写代码易错、字段不一致；六人联调时数据库未初始化、前后端分仓库、启动步骤多，Windows 与 Linux 环境差异大；无图资源展示不统一，影响列表与详情观感。

**创新思路.** 针对联调和展示上的实际问题，做了图片本地化、无图 SVG 占位、从表结构生成代码、启动前检查数据库、一键启动脚本、Docker 部署，并把前后端与 agent 收到同一个仓库里。

**技术方案.** 后端识别 URL、Base64、本地路径三种图片输入并统一落盘，按年/月目录存储，文件名采用时间戳加内容哈希，前端统一图片地址处理函数，避免热链失效。无图标或头像时，按名称首字与哈希配色动态生成 SVG 占位图，零额外存储。Python 脚本读取 `schema.sql`，批量生成 Go 层 Repository 与基础 CRUD，并集成 Swagger 作为联调文档。`run-dev` 脚本顺序执行 dbcheck、Air 热重载后端、前端 dev 服务，可选拉起 Python 智能体；脚本针对 Windows PowerShell 的 UTF-8 BOM 编码问题做了兼容。Docker Compose 编排 MySQL、API 与 Nginx 前端，挂载建表脚本与种子数据，API 配置健康检查。git subtree 将前后端与 agent 目录合并为 Monorepo，统一 README 与《运行说明.md》。

#### （4）管理员难以把握资源全貌与隐性关联

**面临的问题.** 工具、课程、项目分散在多个列表里，管理员缺少总览视角，难以回答「近期提交多少、哪类资源最多、审核通过率如何」；资源之间的标签关系靠人工翻找，难以发现「常一起出现」的组合，也不利于向答辩观众展示平台的数据价值。

**创新思路.** 增加 Insights 管理端：运营大屏汇总各类资源数据，关联图谱按共同标签展示资源之间的关系，不依赖用户点击日志，适合课程项目演示。

**技术方案.** 浏览器并行请求各业务接口并在前端聚合，用 ECharts 绘制 KPI 卡片、资源类型占比、分类排行与时间趋势；关联图谱计算两资源共同标签数量作为关联强度，以力导向图展示，支持缩放、拖拽与点击跳转详情。另补充会话画像、事件时间线、接口遥测、长列表演示等 Insights 子页，用于展示检索行为、审核事件与前端性能观测能力。Insights 路由对普通用户隐藏，仅管理员可访问，与主站权限模型一致。

#### （5）用户提交内容存在安全与质量风险

**面临的问题.** 提交的资源或评论可能含敏感词、危险链接（如 `javascript:` 协议）；重复投稿浪费审核精力；仅在前端拦截可被绕过，存在演示环境下的合规隐患。

**创新思路.** 提交和评论时前后端用同一套敏感词和链接规则各检查一遍；提交前若标题与已有资源太像，先提示用户确认。

**技术方案.** 前端在提交表单时对标题、正文、外链等字段做本地预检，命中敏感词或危险链接协议时即时提示用户修改；后端 Go 在数据写入数据库前再次执行相同规则校验，覆盖工具、课程、项目及评论四类实体，词库与前端保持一致，防止绕过前端直接调接口。重复投稿检测在提交前拉取已有标题，计算文字相似度，超过阈值时弹窗提示用户确认是否仍要提交。详情页相关推荐按标签重叠程度与热度加权，在页面底部展示同类资源。

#### （6）流式问答与多服务联调存在隐蔽工程问题

**面临的问题.** 大模型回答若等全部生成再返回，等待时间长、体验差；Go 反向代理或中间层可能缓冲 SSE 导致「假流式」；向量库与 MySQL 审核状态不同步时，学习助手可能引用已下架内容；Gemini API 或智能体进程不可用时，学习助手直接报错会影响答辩演示。

**创新思路.** 回答用 SSE 逐字返回，Go 转发时避免缓冲；审核状态变了就改索引；智能体或 API 挂了时，还能退回只展示检索结果或 SQL 搜索，答辩时不至于完全用不了。

**技术方案.** Python 智能体用 SSE 推送回答片段，Go 后端逐块转发；审核通过、驳回、删除时更新或删除对应索引。限流防止频繁提问；管理员可查看智能体状态。网络异常时先只返回检索到的段落，再尝试 SQL 搜索，最后给出固定说明文字。

### 2.3 前后端集成与接口规范

主要工作包括：

- 以 OpenAPI 文档作为联调依据，统一三模块（工具、课程、项目）的字段命名、分页参数与状态码约定，减少「同一含义多个字段名」导致的联调返工；
- 封装前端 HTTP 请求模块，区分公开接口与需登录接口：公开接口返回 401 时不强制跳转登录页，避免用户在浏览详情时被误踢出；需登录接口则正常刷新 Token 或引导重新登录；
- 梳理三模块收藏数、评论数、浏览量等统计字段，消除列表页卡片与详情页头部数据不一致的问题；
- 实现应用启动时的登录态初始化、路由守卫（未登录不可访问提交页与 Insights）、按角色展示侧栏菜单（管理员可见运营入口）；
- 学习助手需登录后使用，语料也只包含已审核通过的内容；
- Swagger 文档作为中期联调的主要参照，Repository 脚手架保证各资源类型的数据库访问层结构一致。

### 2.4 RAG 智能体实现补充

#### 2.4.1 首页关键词搜索（与学习助手独立）

首页与统一搜索页面向的是「我已经知道大概要找什么，希望快速筛出列表」这一场景，因此不调用向量库和大模型，而是走传统的关键词加数据库查询路径，保证响应速度和结果可解释性。

具体实现上包括以下三项：

- **同义词扩展**：平台维护一张同义词对照表，当用户输入「IDE」「集成开发环境」等词时，系统会自动把查询扩展为若干相关词一并检索，避免因为表述不同而漏掉本应匹配的资源。
- **检索意图识别**：根据查询词的特征判断用户更可能想找工具、课程还是项目，例如出现「课件」「实验」等词时偏向课程，出现「框架」「开源」等词时偏向项目，识别完成后自动切换到对应标签页，减少用户手动点 Tab 的操作。
- **搜索状态与 URL 同步**：关键词、当前标签页、分页等状态会写入浏览器地址栏，用户刷新页面或通过链接分享时，搜索结果不会丢失，便于联调与演示。

上述逻辑在前端完成预处理后，调用 Go 后端已有的 SQL 列表接口，对标题、简介等字段做模糊匹配并分页返回。这与 RAG 学习助手有明确分工：首页搜索解决「我知道关键词，要快速定位列表」；学习助手解决「我用一整句自然语言描述需求，需要综合多条内容后回答」。

#### 2.4.2 详情页相关推荐与重复投稿提示

用户在查看某一工具、课程或项目的详情时，页面底部会展示「相关推荐」资源条。实现方式是：在同类型资源中，计算当前条目与其他条目标签集合的重叠比例（Jaccard 相似度），重叠越多说明主题越接近；在此基础上再参考收藏量、浏览量等热度指标做加权，取得分最高的若干条展示。这样用户无需返回列表页，就能顺藤摸瓜找到同类资源。

在提交新资源时，系统会在表单提交前拉取已有资源的标题列表，计算新标题与已有标题的文字相似度。若相似度超过设定阈值，则弹出提示「可能存在相似条目，是否仍要提交」，由用户确认后再写入。该机制不能替代人工审核，但能在入口侧挡掉一部分明显重复投稿，减轻审核同学的工作量。上述推荐与去重逻辑均为自主实现的轻量算法，不依赖外部分推荐服务，便于在课程项目环境中说明原理与演示效果。

#### 2.4.3 知识库索引五步骤

知识库构建分为五个连续步骤，保证学习助手引用的内容始终来自站内已审核数据。

**第一步，内容抽取。** 从 MySQL 读取状态为「已通过审核」的工具、课程、项目正文，并将评论区、课程资料等附属文本一并纳入语料，使问答不仅能引用官方说明，也能引用同学讨论中有价值的信息。

**第二步，文本切块。** 单篇正文若较长，按约一千二百字切分为若干段，每段分配唯一编号并计算内容哈希。切块既方便向量检索精确定位，也便于后续某一段内容变更时做增量更新，而不必重建整篇文档。

**第三步，写入向量库。** 对每个文本段调用 Gemini 嵌入接口转为向量，批量写入 Chroma 向量库；若单次调用失败则记录日志并重试，避免索引因网络抖动而残缺。

**第四步，写入关键词索引。** 同一段文本同步写入 SQLite 倒排表，采用自研的中文二元组分词与 BM25 类打分，用于精确匹配工具名、课程名等短查询。

**第五步，自动更新。** 资源审核通过、被驳回、被编辑，或评论、资料发生变更时，Go 后端触发索引 Hook，对向量库与关键词库做增量增删；管理员也可通过后台接口发起全量重建，用于数据校准或答辩前刷新知识库。

#### 2.4.4 一次问答的流程

**表 3 学习助手一次问答的流程**

| 步骤 | 在哪里 | 做什么 |
|------|--------|--------|
| 1 | 浏览器 | 用户输入问题，带上登录信息和多轮对话历史 |
| 2 | Go 后端 | 校验是否登录、是否触发限流 |
| 3 | Python 智能体 | 并行执行关键词搜索与向量搜索 |
| 4 | Python 智能体 | 加权合并、去重，整理成带编号的参考资料 |
| 5 | Python 智能体 | LangChain 拼提示词，约束「仅依据引用回答」，调用 Gemini 生成 |
| 6 | Go 后端 | SSE 流式转发，避免缓冲导致卡顿 |
| 7 | 浏览器 | 逐字显示回答，引用编号可点开跳转详情 |

#### 2.4.5 Go 网关与 Python 智能体分工

为避免智能问答模块拖慢或拖垮主站，系统将 Go 后端与 Python 智能体拆为两个可独立部署的服务。Go 后端承担日常业务与网关职责：用户登录校验、接口限流、从 MySQL 读取业务数据、在审核链路中触发切块与索引更新、将智能体返回的回答以 SSE 方式流式转发给浏览器，并在智能体异常时执行降级兜底。Python 智能体则专注「检索加生成」：维护 Chroma 与 SQLite 两套索引、执行混合检索、用 LangChain 编排提示词、调用 Gemini 生成带引用编号的回答。

两者通过 HTTP 接口通信，智能体可以单独重启、升级或临时关闭，而不影响资源浏览、收藏、提交、审核等核心功能。答辩或联调时若 Python 进程未启动，Go 层仍可返回 SQL 关键词搜索结果或友好提示，保证演示流程不中断。

#### 2.4.6 现成工具与自主开发对照

**表 4 RAG 智能体及相关功能分工**

| 功能 | 现成工具或服务 | 自己写的部分 |
|------|----------------|--------------|
| 首页搜索 | 后端 SQL 列表接口 | 同义词扩展、意图识别、URL 同步 |
| 相关推荐 | — | 标签 Jaccard 相似度与热度加权 |
| 重复投稿提示 | — | 标题相似度检测与确认交互 |
| 向量库 | Chroma | 切块规则、语料范围、审核后自动更新 |
| 文字向量化 | Gemini 嵌入接口 | 批量调用、失败重试与增量写入 |
| 关键词索引 | SQLite | 二元组分词、倒排表、BM25 类打分 |
| 两路结果合并 | — | 加权排序、每资源段数上限、去重 |
| 对话流程 | LangChain | 提示词模板、引用编号格式、多轮历史 |
| 生成回答 | Gemini 生成接口 | 流式解析、无引用时的拒答策略 |
| 请求转发 | Gin | 登录校验、限流、SSE 刷新转发 |
| 索引触发 | — | 审核 Hook、全量重建接口 |

### 2.5 工程化建设实现补充

**外链图片本地化。** 提交表单中的图片字段支持三种来源：外部 HTTP(S) URL、Base64 编码、已有本地路径。后端统一识别并下载或解码，存储到按年/月划分的目录，文件名采用「时间戳 + 内容哈希」避免冲突与重复存储。前端通过统一的图片 URL 处理函数展示，列表页与详情页行为一致，解决热链失效、防盗链与跨域问题。

**SVG 默认图标。** 当工具、课程、项目未上传图标或用户无头像时，根据名称首字与名称哈希生成配色方案，动态渲染 SVG 占位图，不占用磁盘静态资源，与实际上传图标在 UI 上无缝切换，保证列表视觉整齐。

**数据库代码自动生成。** 编写 Python 脚本解析 `schema.sql` 中的表结构，按约定模板批量生成 Go 层 Repository 文件与基础 CRUD 方法，减少三套模块重复手写带来的字段遗漏。配合 Swagger 生成 OpenAPI 文档，供前后端联调时对照。

**dbcheck 自检。** 后端启动前运行 dbcheck 子命令，检测 MySQL 连接与关键业务表是否存在；若未初始化则输出明确提示：先执行建表脚本、再导入演示数据，降低新同学「服务起了但全是 500」的排查成本。

**run-dev 一键启动。** 根目录脚本顺序启动 dbcheck、Air 热重载 Go 后端、Vue 前端 dev 服务，可选启动 Python 智能体。Air 监听 `cmd/`、`internal/` 等目录，保存 Go 文件后自动重新编译运行。脚本对 Windows PowerShell 下 UTF-8 BOM 导致的中文乱码做了处理，保证六人不同操作系统下命令一致。

**Docker Compose 容器化。** 编排 MySQL、Go API、Nginx 静态前端三服务，挂载 SQL 初始化脚本与种子数据，API 配置健康检查探针，前端构建时注入 API 基地址，适合答辩现场「一条 docker compose up」快速演示。

**Monorepo 整合。** 使用 git subtree 将原先分散的前端、后端、agent 仓库合并到同一 Monorepo，统一版本说明、Issue 追踪与《运行说明.md》，避免「克隆三个仓库、对三个分支」的协作摩擦。

**内容安全双端校验。** 维护前后端一致的敏感词与危险链接规则库，提交与评论链路前端预检提升体验，后端强制校验保证不可绕过，覆盖工具、课程、项目及评论实体。

### 2.6 运营可视化与 Insights 管理端

Insights 是管理员用的数据与演示页面，普通用户看不到。主要页面包括：

- **运营数据大屏**：并行拉取工具、课程、项目及审核相关接口，在前端聚合后绘制 KPI 卡片（总量、待审数、通过率等）、资源类型占比饼图、分类排行柱状图及按时间维度的趋势图，支持手动刷新，适合答辩展示平台「有数据、可运营」。
- **资源关联图谱**：基于标签共现构建无向图，节点为资源，边权重为共同标签数量，使用 ECharts 力导向布局展示；可切换布局、点击节点跳转对应详情，体现工具、课程、项目之间的隐性关联，不依赖用户点击日志。
- **会话画像与事件时间线**：用 localStorage 记录本机浏览路由、详情打开次数与搜索词频次，生成简易用户画像页；事件时间线合并提交、审核、评论等操作记录，以 Timeline 组件展示。
- **接口遥测与长列表演示**：在前端请求拦截层统计接口耗时与错误率，提供开发者向的遥测面板；长列表页用虚拟滚动展示万级行数据，说明列表性能优化方案。

上述页面均基于 Vue 3 + Element Plus + ECharts 实现，数据来自真实后端接口或前端聚合，满足课程项目「可截图、可讲解、可演示」的要求。

### 2.7 实施过程中的关键问题与对策

**（1）语义路与关键词路如何平衡。** 不同类型的提问适合不同的检索方式：例如「有没有 Vue 相关的课程」更适合向量语义路，「Git 版本控制工具有哪些」则更依赖关键词精确匹配。若只保留一路，前者容易漏掉短词专名，后者又听不懂换说法的提问。解决方式是对两路得分做加权合并，并限制同一资源最多贡献几段引用，在开发阶段用一批典型问句反复试权重，直到结果既不过于集中也不过于发散。

**（2）SSE 流式回答被中间层缓冲。** 现象是用户提问后长时间无输出，随后整段答案一次性弹出，失去「打字机」体验。排查发现 Go 转发层或浏览器端可能缓冲了 SSE 数据。解决方式是在 Go 侧每收到一块数据立即 Flush，并设置禁用缓冲的响应头；前端按 SSE 事件边界逐条解析，保证 token 级渲染。

**（3）知识库与审核状态不一致。** 早期曾出现资源已被驳回或下架，但向量库中仍可检索到对应片段的情况。后在审核通过、驳回、删除等 Service 链路挂载索引 Hook，对变更资源增量删除或更新对应 chunk；管理员亦可手动触发全量重建，用于答辩前校准知识库与数据库的一致性。

**（4）外部 API 或智能体不可用。** 联调与答辩时网络不稳定，Gemini 或 Python 进程偶尔会超时。处理办法是：先尝试正常问答；超时则只返回检索到的段落并说明原因；还不行就用 Go 层 SQL 搜索；最后给出一段固定说明，标明当前模式，避免演示完全中断。

### 2.8 收获、不足与总结

**收获.**

- 完成了学习助手从建索引、混合检索到前端问答的完整流程；
- 熟悉了 Go 业务服务和 Python 智能体如何分工配合；
- 理清了首页搜索、详情推荐和学习助手各自适用的场景；
- 通过 Monorepo、Docker 和 run-dev 脚本，组内联调方便了很多；
- 做了 Insights 大屏和图谱，积累了 ECharts 和前端聚合数据的实践经验。

**不足与展望.**

- 首页搜索仍以规则和同义词为主，以后可以考虑和学习助手共用检索能力；
- 嵌入和生成依赖 Gemini 在线接口，可尝试换本地模型；
- 中文分词比较简单，专业术语召回还有提升空间；
- 自动化测试较少，主要靠手工联调；
- WebSocket 通知、OpenAPI 自动生成 SDK 等只做到演示程度。

**总结.** 我主要负责学习助手、搜索、工程化脚本、Insights 大屏和前后端联调等工作。上述功能已在项目中实现，答辩时可用 run-dev 或 Docker Compose 启动演示。

---

*文档版本：2026-07-05*
