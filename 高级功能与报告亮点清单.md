# 高级功能与报告亮点清单（课程后续 / 报告导向）

> **目标**：在报告中体现「系统性强、技术栈新、工程量大、有创新点」——**不必**追求登录与权限真实落地（课程约定可不做）。  
> **工程范围**：以前端 `softeng-platform-frontend/` 与后端 `softeng-platform/softeng-platform/` 为主；能演示、能截图、能写进「设计与实现」章节即可，部分可走 **Mock / 静态聚合 / 只读接口**。

---

## 一、明确不做的范围（写进报告「约束与假设」即可）

| 项 | 说明 |
|----|------|
| 真实用户体系落地 | 注册登录、Token 续期、风控等仅作背景，不投入实现与测试 |
| 生产级安全合规 | 等保、审计留痕、敏感词全量审核等可列为「未来工作」 |
| 全链路真实数据 | 允许「演示数据集 + 聚合接口」混用，报告中注明为原型验证环境 |

---

## 二、高级功能矩阵（建议直接对应报告「功能模块图 / 用例表」）

下列每一项都可在报告中展开为：**需求 → 方案 → 关键技术 → 界面/接口 → 工作量估计**。

| 编号 | 功能域 | 高级能力（报告用语） | 可落地的技术抓手 | 与现有项目结合点 |
|------|--------|----------------------|------------------|------------------|
| A01 | 智能检索 | **多模态检索 / 语义检索**（课程资源、工具、项目统一入口） | 前端：同义词扩展、标签权重、高亮分词；可选接开源嵌入模型 API 做「语义排序」演示 | 首页搜索、各列表 `search` 参数聚合 |
| A02 | 智能检索 | **检索意图识别**（工具 / 课程 / 项目 Tab 自动跳转） | 规则引擎 + 简单关键词分类；可加「查询日志」面板 | `SearchPage.vue`、Vuex 搜索状态 |
| A03 | 知识图谱 / 关联 | **资源关联网络**（工具—课程—项目共现、同标签聚类） | 前端力导向图（D3 / ECharts Graph）或静态 JSON 生成图数据 | 工具/课程/项目详情侧栏「相关推荐」 |
| A04 | 推荐系统 | **混合推荐**（热门 + 协同过滤演示 + 内容相似） | 后端聚合接口：基于标签 Jaccard / 共现计数；前端「猜你喜欢」卡片 | 详情页底部、首页瀑布区块 |
| A05 | 个性化 | **用户画像看板**（浏览轨迹、偏好标签云）——**不落登录**则用「本机会话画像」 | `localStorage` 事件埋点 + 画像 JSON + 可视化 | 新建 `Analytics` 或嵌入个人中心子页（演示模式） |
| A06 | 数据可视化 | **资源运营大屏**（提交量、审核通过率、分类分布、时间序列） | ECharts / Apache ECharts；数据来自现有表或 Mock SQL 结果 JSON | 管理端扩展页或独立 `/dashboard` |
| A07 | 数据可视化 | **学习路径甘特图 / 里程碑**（与课程模块挂钩） | 时间轴组件 + 静态阶段数据；报告写「教学计划可视化」 | 课程详情 Tab |
| A08 | 工作流 | **审核流状态机可视化**（待审→通过/驳回及原因） | 前端状态图（Mermaid 渲染或自定义 Step） | `audit.vue` 增强 |
| A09 | 工作流 | **批量审核 / 批量导出**（演示级） | 表格多选 + 批量请求队列 + 进度条 UI | 审核列表页 |
| A10 | 内容智能 | **摘要与要点自动生成**（工具说明、课程简介） | 调用公开摘要 API 或本地 `extractKeywords` 规则 + 模板句 | 详情页「AI 摘要」折叠面板 |
| A11 | 内容智能 | **相似资源去重提示**（编辑提交时） | 标题 Levenshtein / 指纹 SimHash 演示 | 各 `Submit` 页提交前校验 UI |
| A12 | 协作与通知 | **站内「事件时间线」**（提交、审核、评论聚合） | 统一 Timeline 组件 + 合并多资源类型事件（可先 Mock） | 新建时间线页或详情侧栏 |
| A13 | 实时性（演示） | **WebSocket 推送「审核结果通知」** | 可用 `mock-socket` 或后端极简 `ws` 广播；报告写「准实时通知架构」 | 顶部铃铛 + 消息列表 |
| A14 | 性能工程 | **虚拟滚动长列表**（万级数据演示） | `vue-virtual-scroller` 或 Element 大表格虚拟化 | 工具/项目超长列表演示页 |
| A15 | 性能工程 | **前端缓存与 SWR**（Stale-While-Revalidate） | 列表内存缓存 + 后台静默刷新；报告写「体验与一致性权衡」 | Vuex 或轻量 composable |
| A16 | 可观测性 | **前端可观测性面板**（性能指标、错误率、慢接口） | `PerformanceObserver` + 请求耗时统计 + 简易图表 | 开发者面板（仅 `import.meta.env.DEV` 或开关） |
| A17 | 国际化 / 无障碍 | **双语切换 + 键盘导航** | `vue-i18n` 最小集；`focus-visible`、ARIA 标签 | 全局设置抽屉 |
| A18 | 文档与工程 | **OpenAPI 驱动的前端 SDK / 类型** | 从现有或补全 Swagger 生成 `api-types`；报告写「契约优先」 | `src/api` 重构叙事 |
| A19 | DevOps 叙事 | **容器化与一键编排说明** | `Dockerfile` + `docker-compose`（MySQL + 前后端）；报告配架构图 | 仓库根目录文档 + 脚本 |
| A20 | 安全叙事（不写登录） | **内容安全扫描演示**（链接黑名单、敏感词本地词库） | 提交前前端校验 + 后端占位中间件 | 提交接口前后装饰 |
| **A21** | **大模型与知识库** | **RAG 问答助手**（先检索平台资源片段，再 LLM 生成或离线模板回答；引用溯源） | MySQL `LIKE` 多表检索；OpenAI 兼容 `chat/completions`；无 Key 时 demo 模式 | `POST /api/agent/rag`、`internal/service/rag_agent.go`、`RagAgent.vue` |
| **A22** | **开发体验** | **后端 Air 热重载 + 根目录一键启动**（改 Go 即自动重编；单命令拉起 API 与前端） | **Air** 监听 `cmd/`、`internal/`、`pkg/`；`run-dev.ps1` / `run-dev.bat` 新开窗口跑 `air` + `npm run serve` | `.air.toml`、`start-dev.ps1`、仓库根 `run-dev.ps1` |

### 二点一、编号与仓库实现状态（答辩核对）

| 编号 | 状态 | 说明 |
|------|------|------|
| A01 | **已实现（规则层）** | 同义词与扩展在 `smartSearch.js`；统一搜索入口与参数传递；**未接**真实向量嵌入。 |
| A02 | **已实现** | `SearchPage.vue` 意图 Tab、`detectSearchIntent`；`store` 中 `doSearch` 带 `type`；URL `query.type` 同步。 |
| A03 | **已实现** | `/insights/knowledge-graph`，`KnowledgeGraph.vue`，ECharts Graph。 |
| A04 | **已实现** | `ResourceRecommendBar.vue`，详情页挂载；多源列表加权。 |
| A05 | **已实现** | `/insights/session-persona`，`SessionPersona.vue`；`sessionAnalytics.js`；`router.afterEach` 写入浏览与搜索词。 |
| A06 | **已实现** | `/insights/dashboard`，`OperationsDashboard.vue`。 |
| A07 | **已实现** | 课程详情内嵌 `CourseLearningPath.vue`：静态周次甘特条 + `el-timeline` 里程碑（哈希分班）。 |
| A08 | **已实现** | `audit.vue` 审核步骤与 SLA 示意。 |
| A09 | **已实现** | `audit.vue`：多选 + **批量通过 / 批量拒绝**（统一原因），本页全选。 |
| A10 | **已实现（离线规则）** | `ResourceAiSummary.vue`、`contentInsight.js`；报告可写「可替换为 LLM」。 |
| A11 | **已实现** | `stringSimilarity.js`；`ToolSubmit.vue` / `ProjectSubmit.vue` 提交前 **相似标题** 检索确认。 |
| A12 | **已实现** | `/insights/event-timeline`，`EventTimeline.vue`；`eventJournal.js` 与路由浏览合并展示。 |
| A13 | **已实现（演示 + 可选真 WS）** | 本地通知队列 + 定时注入；首页铃铛；审核通过推送；**`GET /api/ws/stream`**（Gorilla WebSocket 心跳 JSON）；首页自动连接并写入通知。 |
| A14 | **已实现** | 工具列表 `content-visibility`；`/insights/long-list` 万级演示 `LongListDemo.vue`。 |
| A15 | **部分（叙事 + 方案）** | `TelemetryPanel.vue` 中 SWR 说明；与运营大屏刷新叙事衔接。 |
| A16 | **已实现** | `apiMetrics.js`、`request.js` 拦截耗时；`/insights/telemetry`。 |
| A17 | **已实现（最小集）** | `vue-i18n@9`：`src/i18n` + 首页侧栏/分区标题/搜索引擎名中英文切换。 |
| A18 | **部分** | `src/types/api.d.ts` 提供 RAG 等响应形状；完整 OpenAPI 生成 SDK 仍为可选增强。 |
| A19 | **已实现** | 根目录 `docker-compose.yml`；前后端 `Dockerfile`；前端 `nginx.conf`（SPA `try_files` + gzip）。 |
| A20 | **已实现（前后端双端）** | 前端 `contentSafety.js`；后端 **`internal/pkg/safety/content.go`**：工具/项目/课程资料提交与 **评论** 写入前校验（敏感词 + 危险链接协议，与前端词库对齐）。 |
| **A21** | **已实现** | 全栈 RAG：`/insights/rag-agent`（**仅管理员入口**）；**`POST /api/agent/rag`** 需登录且管理员；检索 + **Gemini / OpenAI / demo**；回退文案密钥脱敏。 |
| **A22** | **已实现** | **Air** 热重载；根目录 **`run-dev.ps1`/`run-dev.bat`**（UTF-8 BOM）；**`go run ./cmd/dbcheck`**；**`bootstrap_user.sql`**。 |

---

## 三、报告里「显得工作量大」的写法模板（每条 1 段即可）

1. **背景与痛点**：资源分散、检索弱、审核不透明、运营无数据。  
2. **总体方案**：分层架构 + 模块 X + 技术 Y（引用上表编号）。  
3. **关键算法 / 流程**：配流程图或时序图（Mermaid）。  
4. **实现与验证**：截图 + 关键代码路径 + 可选性能数字（前后对比）。  
5. **创新点**：与同类课程项目对比的 1～2 条（如「统一检索 + 图谱关联 + **RAG 引用溯源**」）。

---

## 四、建议实施顺序（优先「能写章节 + 能演示截图」）

| 阶段 | 包含编号 | 说明 |
|------|-----------|------|
| P1 报告骨架 | A06、A08、A03 | 大屏 + 审核流 + 关联图，视觉冲击强 → **仓库已完成** |
| P2 智能叙事 | A01、A02、A04、A10、**A21** | 检索与推荐、摘要、**RAG** → **仓库已完成（A21 为新增亮点）** |
| P3 工程深度 | A14、A15、A16、A18、A19 | 性能、观测、契约、部署 → **A14/A16/A19 已完成；A15 叙事；A18 已加 `api.d.ts` 示例** |
| P4 可选加分 | A05、A12、A13、A17、A20 | 画像、时间线、实时、国际化、安全扫描 → **本仓库已按演示级落地** |

---

## 五、原「纯外观」项（降级为辅助，仍可在报告「人机交互」一节简要写）

统一设计 token、骨架屏、404 页、路由过渡、响应式侧栏等——作为 **HCI 与体验优化** 小节，不必单独撑起工作量叙述；与上表合并写即可。

---

*文档说明：以课程报告与答辩演示为主；登录与账号体系按课程要求可不实现。后续实现某条时，可在本表「编号」旁标注完成日期或链接到 PR/Commit。*

---

## 六、迭代记录（与仓库实现同步 · 汇总版）

> 下列为当前仓库已落地的 **全部** 相关改动与优化，可按编号写入报告「迭代与版本说明」。

| 日期 | 编号 / 类别 | 内容 |
|------|----------------|------|
| 2026-05-12 | **A06** | 运营数据大屏：`/insights/dashboard`，`OperationsDashboard.vue`；ECharts 多图、KPI、刷新；侧栏「运营大屏」。 |
| 2026-05-12 | **A03** | 资源关联图谱：`/insights/knowledge-graph`，`KnowledgeGraph.vue`；力导向 / 环形布局；侧栏「关联图谱」。 |
| 2026-05-12 | **A08** | 审核中心增强：`audit.vue`，`el-steps`、SLA 示意与演示交互。 |
| 2026-05-12 | 依赖 | 前端 `echarts@5`；路由 `/insights/*`（**仅管理员**；普通用户侧栏不可见，直链会重定向 `/home`）。 |
| 2026-05-12 | **A01 / A02** | 智能检索：`SearchPage.vue` 意图 Tab、同义词芯片、意图提示；`smartSearch.js`；`store` 的 `doSearch` 传 `type`；URL `type` 与 `submitSearch` / `performSearch` 分离，避免 **router watch 循环**。 |
| 2026-05-12 | **A04** | 混合推荐条：`ResourceRecommendBar.vue`；工具/课程/项目详情引用。 |
| 2026-05-12 | **A10** | 智能摘要：`ResourceAiSummary.vue`、`contentInsight.js`（离线规则摘要/要点）。 |
| 2026-05-12 | **A14** | 性能：`ToolsList.vue` 卡片 **`content-visibility`**；`/insights/long-list` + `LongListDemo.vue` 万级行演示；列表页链到长列表说明。 |
| 2026-05-12 | **A15** | `TelemetryPanel.vue` 内 **SWR** 叙事说明（与列表缓存、运营大屏刷新衔接）。 |
| 2026-05-12 | **A16** | 可观测性：`src/utils/apiMetrics.js`；`src/api/request.js` 请求耗时写入环缓；`/insights/telemetry` + `TelemetryPanel.vue`（Performance、内存、`performance.memory`、HTTP 样本表）。 |
| 2026-05-12 | **A19** | 根目录 `docker-compose.yml`（MySQL 健康检查、挂载 `schema.sql` 初始化、api、web）；后端 `softeng-platform/softeng-platform/Dockerfile`（多阶段构建 `cmd/server`）；前端 `Dockerfile` + `nginx.conf`（`try_files` SPA、gzip json/js）。 |
| 2026-05-12 | **A21 RAG（全栈）** | **后端**：`internal/config/config.go` 增加 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`；`internal/service/rag_agent.go` 多表 `LIKE` 检索（工具仅 `approved`，课程/项目按表结构）；OpenAI 兼容 Chat Completions；无 Key **demo** 模板回答仍带 `sources`。**Handler**：`internal/handler/rag_agent.go`。**路由**：`POST /api/agent/rag`（`cmd/server/main.go`，无需登录）。**前端**：`/insights/rag-agent`，`RagAgent.vue`；`HttpManager.ragAgentChat`；`request.js` 新增 **`postJSON`**（避免全局 `x-www-form-urlencoded` 破坏 JSON）；401 拦截将 **`api/agent/rag`** 视为公开 POST，避免误登出。侧栏「**RAG 助手**」。 |
| 2026-05-12 | 后端工程 | Swagger 依赖与 `go get` 相关修正（保证 `swag` 文档路由可编译）；用户本地 **`.env` + MySQL** 连接问题属环境配置，报告可写「通过环境变量配置数据源」。 |
| 2026-05-12 | 前端工程 | ESLint：`OperationsDashboard` 中 ECharts **`dispose`** 空块等按规范简化。 |
| 2026-05-12 | **A05** | 会话画像：`/insights/session-persona`；`sessionAnalytics.js`；路由 `afterEach` 聚合访问与搜索词。 |
| 2026-05-12 | **A07** | 课程详情学习路径：`CourseLearningPath.vue`（甘特式周次条 + 时间线）。 |
| 2026-05-12 | **A09** | 审核中心批量裁定：`audit.vue` 多选、全选本页、批量通过/拒绝、日志与演示通知联动。 |
| 2026-05-12 | **A11** | 提交相似度：`stringSimilarity.js` + 工具/项目提交前搜索近似名二次确认。 |
| 2026-05-12 | **A12** | 事件时间线：`/insights/event-timeline`；`eventJournal.js` 与通知列表合并。 |
| 2026-05-12 | **A13** | 演示通知：`demoNotifications.js`；首页铃铛；定时推送文案；审核通过写入通知。 |
| 2026-05-12 | **A17** | `vue-i18n`：`src/i18n`、`main.js` 注册；首页侧栏 `i18nKey`、分区标题、引擎「本站」双语。 |
| 2026-05-12 | **A18** | `src/types/api.d.ts`：`RagChatResponse` 等契约类型示例。 |
| 2026-05-12 | **A20 后端** | `internal/pkg/safety/content.go`；`SubmitTool`/`UpdateTool`、项目上传/更新、课程资料上传、**三类评论** 前统一校验。 |
| 2026-05-12 | **A13 真 WS** | `github.com/gorilla/websocket`；`handler/ws_demo.go`；`GET /api/ws/stream`；`main.go` 注册；前端 `wsDemo.js` + 首页连接关闭生命周期。 |
| 2026-05-12 | 工程串联 | 提交/审核成功写 `appendJournalEvent`；路由浏览自动写 journal；侧栏新增「会话画像」「事件时间线」。 |
| 2026-05-14 | **A22** | Air 热重载说明与 `start-dev` 走 `cmd /c air`；根目录 **`run-dev.ps1`/`run-dev.bat`**；**`cmd/dbcheck`** 检查 MySQL 表；**`database/bootstrap_user.sql`** 对齐默认 `softeng_app`。 |
| 2026-05-14 | **上线加固（权限）** | 洞察侧栏项 **`adminOnly`**；路由 **`meta.requiresAdmin`**；**`POST /api/agent/rag`** 与 **`/admin/*`** 一致需 **Bearer + admin/superadmin**；**`GET /api/ws/stream`** 需 **`?token=`** 且管理员；`AdminMiddleware` 支持 **superadmin**；回退文案 **密钥脱敏**（`rag_agent.go`）。 |
| 2026-05-14 | **上线加固（CORS / 体验）** | **`CORS_ALLOWED_ORIGINS`** 环境变量 + `middleware.CORS(allowed)`；课程/工具/项目列表 **加载中 / 失败重试 / 空态**（`CourseList`、`ToolsList`、`ProjectList`）。 |
| 2026-05-14 | **上线加固（探针 / 编排）** | **`GET /live`**、**`GET /health`**（MySQL Ping）；`handler/health.go`；Compose **`api.healthcheck`**（`wget`）、**`web` 依赖 `api: service_healthy`**；API 镜像安装 **wget**。 |
| 2026-05-14 | **上线加固（限流 / 日志 / 前端构建）** | **`/auth` 全组** `AuthRateLimit`（每 IP 45 次/分钟）；**`GIN_MODE` / `APP_ENV=production`** 进入 release、关闭逐请求 `[DEBUG]`；前端 **`VUE_APP_API_BASE`**（`config.js`、`.env.development`、`.env.example`、前端 **Dockerfile ARG**、Compose **`web.build.args`**）。 |

---

## 七、报告附录：主要文件路径速查

### 7.1 前端（`softeng-platform-frontend/`）

| 类型 | 路径 |
|------|------|
| 洞察路由 | `src/router/index.js`（`/insights/...` **`requiresAdmin`**；`/check/audit` 同） |
| 侧栏入口 | `src/store/index.js`（`visibleSidebarItems`：**`adminOnly`** 洞察项 + 审核中心；**`isAdmin`** 含 superadmin） |
| **会话画像** | `src/pages/insights/SessionPersona.vue`、`src/utils/sessionAnalytics.js` |
| **事件时间线** | `src/pages/insights/EventTimeline.vue`、`src/utils/eventJournal.js` |
| **学习路径** | `src/components/CourseLearningPath.vue`（嵌入 `CourseDetail.vue`） |
| **内容安全** | `src/utils/contentSafety.js` |
| **相似标题** | `src/utils/stringSimilarity.js` |
| **演示通知** | `src/utils/demoNotifications.js` |
| 国际化 | `src/i18n/index.js`、`src/i18n/locales/zh.json`、`en.json` |
| **演示 WS** | `src/utils/wsDemo.js`；**仅管理员且存在 token** 时首页 `home.vue` 连接并带 **`?token=`** |
| 类型示例 | `src/types/api.d.ts` |
| 运营大屏 | `src/pages/insights/OperationsDashboard.vue` |
| 知识图谱 | `src/pages/insights/KnowledgeGraph.vue` |
| 可观测性 | `src/pages/insights/TelemetryPanel.vue`、`src/utils/apiMetrics.js` |
| 长列表 | `src/pages/insights/LongListDemo.vue` |
| **RAG 对话** | `src/pages/insights/RagAgent.vue` |
| 搜索与意图 | `src/pages/SearchPage.vue`、`src/utils/smartSearch.js` |
| 摘要 / 推荐 | `src/components/ResourceAiSummary.vue`、`ResourceRecommendBar.vue`、`src/utils/contentInsight.js` |
| HTTP 与指标 | `src/api/request.js`（拦截器、`post`、`postJSON`）、`src/api/index.js`（`HttpManager`） |
| API 基地址 | `src/api/config.js`（**`VUE_APP_API_BASE`** 构建注入，默认 `http://localhost:8080`）；`.env.development`、`.env.example` |
| 工具列表优化 | `src/pages/tool/ToolsList.vue`（加载 / 失败 / 空态） |
| 课程列表 | `src/pages/course/CourseList.vue`（加载 / 失败重试 / 空态） |
| 项目列表 | `src/pages/project/ProjectList.vue`（**`projectsLoading`** 与空态分离） |
| 容器与静态资源 | `Dockerfile`（**`ARG VUE_APP_API_BASE`**）、`nginx.conf` |

### 7.2 后端（`softeng-platform/softeng-platform/`）

| 类型 | 路径 |
|------|------|
| 服务入口 | `cmd/server/main.go`（`initGinMode`、`CORS`、`/live`、`/health`、Swagger、`/auth` 限流） |
| RAG 业务 | `internal/service/rag_agent.go` |
| RAG HTTP | `internal/handler/rag_agent.go` |
| **内容安全（Go）** | `internal/pkg/safety/content.go` |
| **WebSocket 演示** | `internal/handler/ws_demo.go` |
| 配置 | `internal/config/config.go`（DB、JWT、Gemini/OpenAI、**`CORSAllowedOrigins`**） |
| **CORS** | `internal/middleware/cors.go`（白名单参数化） |
| **认证限流** | `internal/middleware/ratelimit.go`（`/auth` 组） |
| **健康检查** | `internal/handler/health.go`（`/live`、`/health`） |
| **JWT 中间件** | `internal/middleware/auth.go`（**`AdminMiddleware`**：`admin` / `superadmin`） |
| 数据库 | `database/schema.sql`、`database/bootstrap_user.sql`（应用账号）、`internal/repository/database.go` |
| **库表自检** | `cmd/dbcheck/main.go`（`go run ./cmd/dbcheck`，在后端目录执行） |
| **热重载** | `.air.toml`、`start-dev.ps1` / `start-dev.bat`（`README-AIR.md`） |
| 镜像构建 | `Dockerfile` |

### 7.3 编排与根目录

| 类型 | 路径 |
|------|------|
| 一键编排 | 仓库根目录 `docker-compose.yml`（**api `healthcheck`**、**`web.build.args.VUE_APP_API_BASE`**、`web` **待 api healthy**；`APP_ENV` / `CORS_ALLOWED_ORIGINS` 注释示例） |
| **本地一键双端** | 仓库根目录 **`run-dev.ps1`**、**`run-dev.bat`**（检查表 + Air + `npm run serve`） |

---

## 八、环境变量与接口速查（报告「部署与配置」节）

| 变量 / 接口 | 说明 |
|---------------|------|
| `DB_HOST` `DB_PORT` `DB_USER` `DB_PASSWORD` `DB_NAME` | MySQL 连接；`docker-compose` 内指向 `mysql` 服务。 |
| `PORT` | 后端监听，默认 `8080`。 |
| `JWT_SECRET` | JWT 签名密钥（生产务必更换）。 |
| `VUE_APP_API_BASE` | **前端构建** API 根地址；本地 `.env.development`；Docker 见前端 **Dockerfile `ARG`**、Compose **`web.build.args`**。 |
| `CORS_ALLOWED_ORIGINS` | 逗号分隔的浏览器 **Origin** 白名单；空则使用代码内本地开发默认（`localhost:3000/3001` 等）。 |
| `APP_ENV` | 设为 **`production`** 时 Gin 使用 **release** 模式并关闭逐请求调试日志（与 `GIN_MODE` 二选一配置即可）。 |
| `GIN_MODE` | 设为 **`release`** 或 **`test`** 时直接 `gin.SetMode`（标准 Gin 行为）。 |
| `GOOGLE_API_KEY` / `GEMINI_*` | 非空则 RAG 优先走 **Gemini** `generateContent`（与 blog 对齐）；详见 `internal/config/config.go`。 |
| `OPENAI_API_KEY` | 非空可作 RAG 次选 **OpenAI 兼容**；皆空则 **demo** 模式仍有检索与溯源。 |
| `OPENAI_BASE_URL` | 默认 `https://api.openai.com/v1`；兼容国内转发网关。 |
| `OPENAI_MODEL` | 默认 `gpt-4o-mini`。 |
| `POST /api/agent/rag` | 需 **`Authorization: Bearer`** 且 **管理员**；JSON：`{"message":"…"}`；返回含 **`mode`**：`gemini` / `llm` / `demo`。 |
| `GET /api/ws/stream` | **管理员**演示 WebSocket；Query **`token=<JWT>`**（浏览器 WS 不便带 Header）；心跳 JSON 推送。 |
| `GET /live` | **存活探针**，不访问数据库，`200` + `{"status":"ok"}`。 |
| `GET /health` | **就绪探针**，短超时 Ping MySQL；成功 `200` + `database: up`；失败 **`503`**。 |

---

## 九、可写入报告的「优化点」一句话清单（非功能编号）

- **检索体验**：意图分流、同义词、URL 与搜索逻辑解耦防循环。  
- **详情页增值**：离线摘要条、跨资源推荐条，体现「内容智能 + 推荐」双层叙事。  
- **性能与可观测**：长列表演示、`content-visibility`、HTTP 延迟环缓与面板、Performance API。  
- **工程化交付**：双容器 + Compose + 初始化 SQL，nginx SPA 与压缩；**本地一键 `run-dev` + Air 热重载 + `dbcheck` 表结构自检**。  
- **国际化**：`vue-i18n` 最小集，首页侧栏与常用区块中英切换。  
- **安全与质量**：敏感词与危险链接协议校验；标题与库内资源相似度提示。  
- **运营闭环**：审核批量处理、通过结果进通知队列、事件时间线可截图写报告。  
- **上线与权限（2026-05-14 起）**：洞察与 RAG、WS、审核路由 **前后端管理员一致**；**CORS 可配置**；**`/auth` 限流**；**`/live` `/health` 探针**；列表页 **加载/失败/空态**；**`VUE_APP_API_BASE`** 与 Compose **构建参数 / 健康依赖链**。

---

## 十、上线与工程加固汇总（答辩「非功能需求 / 部署」可引用）

| 主题 | 实现要点 | 主要路径 |
|------|----------|----------|
| 角色与菜单 | 洞察项 **`adminOnly`**；`visibleSidebarItems` 按 **`isAdmin`** 过滤 | `src/store/index.js`、`src/pages/home.vue` |
| 路由守卫 | **`/insights/*`**、**`/check/audit`** `meta.requiresAdmin`；未登录跳转登录；非管理员 **`/home`** | `src/router/index.js` |
| 后端鉴权 | **`POST /api/agent/rag`**：`Auth` + **`Admin`**；**`/admin/*`** 已含 `AdminMiddleware`（扩展 **superadmin**） | `cmd/server/main.go`、`internal/middleware/auth.go` |
| WebSocket | **`GET /api/ws/stream?token=`** + JWT 角色校验；首页仅管理员发起连接 | `internal/handler/ws_demo.go`、`src/utils/wsDemo.js`、`src/pages/home.vue` |
| 前端 401 行为 | **`api/agent/rag`** 不再视为「公开 POST」误触发登出 | `src/api/request.js` |
| CORS | 环境变量 **`CORS_ALLOWED_ORIGINS`**（逗号分隔）；中间件按白名单回写 `Origin` | `internal/config/config.go`、`internal/middleware/cors.go` |
| 运行模式与日志 | **`GIN_MODE`** / **`APP_ENV=production`** → release；release 下关闭逐请求 **`[DEBUG]`** 与 **NoRoute** 调试日志 | `cmd/server/main.go` |
| 认证限流 | **`/auth`** 组：**每 IP 45 次/分钟**，超限 **429** | `internal/middleware/ratelimit.go` |
| 健康检查 | **`GET /live`**（无 DB）、**`GET /health`**（Ping MySQL，2s 超时） | `internal/handler/health.go` |
| 编排 | Compose **`api.healthcheck`**（`wget`）；**`web`** 依赖 **`api: service_healthy`**；API 镜像 **`wget`** | `docker-compose.yml`、后端 **`Dockerfile`** |
| 前端 API 基址 | **`VUE_APP_API_BASE`**；`.env.development`、`.env.example`；前端镜像 **ARG**；Compose **`web.build.args`** | `src/api/config.js`、前端 **`Dockerfile`** |
| 列表体验 | 课程/工具/项目列表：**加载中**、**失败文案 + 重试**、**空数据提示**（项目区分加载与空） | `CourseList.vue`、`ToolsList.vue`、`ProjectList.vue` |

---

*最后更新：2026-05-14（十、上线与工程加固汇总；八章环境/接口与七章路径已同步；变更台账追加 2026-05-14 多条）。*
